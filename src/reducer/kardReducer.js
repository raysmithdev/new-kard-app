import * as actions from '../actions';

const initialState = {
  loading: false,
  error: null,
};

const kardReducer = (state = initialState, action) => {
  switch (action.type) {


  default:
    return state;
  }
};

export default kardReducer;
